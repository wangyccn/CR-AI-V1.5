{
  "model": {
    "vocab_size": 50000,
    "dim": 1024,
    "num_layers": 32,
    "num_heads": 16,
    "num_experts": 8
  },
  "training": {
    "batch_size": 32,
    "lr": 1e-4,
    "epochs": 100,
    "data_path": "data/train.json",
    "save_dir": "checkpoints",
    "save_interval": 5
  }
}